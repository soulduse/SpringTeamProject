<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE sqlMap PUBLIC "-//iBATIS.com//DTD SQL Map 2.0//EN" "http://ibatis.apache.org/dtd/sql-map-2.dtd">
<sqlMap namespace="Data">
	<typeAlias alias="member" type="com.listen.member.vo.MemberVo" />
	<typeAlias alias="bbs" type="com.listen.bbs.vo.BbsViewVo" />
	<typeAlias alias="bbsfile" type="com.listen.bbs.vo.BbsFileVo" />

	<!-- 전체 File 자료 출력 -->
	<select id="getBbsFileList" resultClass="bbsfile" parameterClass="bbs">
		SELECT 
				BBS_FILE_SEQ SEQ, PATH, ORG_NAME, SAVE_NAME, FILE_SIZE
		FROM 
				BBS_FILE
		WHERE
				BBS_SEQ = #bbs_seq#
	</select>
	
	<!-- 파일 등록 -->
	<insert id="uploadFile">
		INSERT INTO BBS_FILE
		(BBS_FILE_SEQ, BBS_SEQ, PATH, ORG_NAME, SAVE_NAME, FILE_SIZE, 
		DISP_YN, REG_EMAIL, REG_DATE, REG_IP, UPD_EMAIL, UPD_DATE, UPD_IP)
		VALUE
		(BBS_FILE_SEQ.NEXTVAL, #bbs_seq#, #path#, #org_name#, #save_name#,
		#file_size#, 'N', 		
		)
	</insert>
	
	<!-- 파일 업데이트 -->
	<insert id="deleteFile">
	
	</insert>
	
	<!-- 파일 삭제 -->
	<insert id="updateFile">
	
	</insert>

</sqlMap>
